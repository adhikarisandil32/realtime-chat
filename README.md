# Intro

A simple realtime messaging app

# Steps to Start

1. Clone the project
2. From the `backend` directory, hit `pnpm dev` command to start the development server of the backend of the app
3. From the `frontend` directory, hit `pnpm dev` command to start the development server of the frontend of the app
